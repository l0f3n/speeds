[project]
name = "speedcomparison"
version = "0.1.0"
description = "Visualize transfer speed differences between storage mediums and protocols using Plotly"
requires-python = ">=3.8"
dependencies = [
    "matplotlib>=3.7.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "plotly>=5.18.0",
]
